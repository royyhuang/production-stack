---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: productionstacks.serving.vllm.ai
spec:
  group: serving.vllm.ai
  names:
    kind: ProductionStack
    listKind: ProductionStackList
    plural: productionstacks
    shortNames:
    - ps
    singular: productionstack
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ProductionStack is the Schema for the productionstacks API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ProductionStackSpec defines the desired state of ProductionStack.
            properties:
              deploymentStrategy:
                default: RollingUpdate
                description: Strategy defines the deployment strategy to use
                enum:
                - RollingUpdate
                - Recreate
                type: string
              env:
                description: Env is a list of environment variables to set
                items:
                  description: EnvVar represents an environment variable
                  properties:
                    name:
                      description: Name is the name of the environment variable
                      type: string
                    value:
                      description: Value is the value of the environment variable
                      type: string
                  required:
                  - name
                  type: object
                type: array
              lmcacheConfig:
                description: LMCacheConfig defines the LM Cache configuration
                properties:
                  cpuOffloadingBufferSize:
                    default: 4Gi
                    description: CPUOffloadingBufferSize is the size of the CPU offloading
                      buffer
                    type: string
                  diskOffloadingBufferSize:
                    default: 8Gi
                    description: DiskOffloadingBufferSize is the size of the disk
                      offloading buffer
                    type: string
                  enabled:
                    default: false
                    description: Enabled enables LM Cache
                    type: boolean
                  remoteSerde:
                    description: RemoteSerde is the serialization format for the remote
                      cache
                    type: string
                  remoteUrl:
                    description: RemoteURL is the URL of the remote cache server
                    type: string
                type: object
              model:
                description: Model defines the model configuration
                properties:
                  chatTemplate:
                    description: ChatTemplate specifies the chat template to use
                    type: string
                  dtype:
                    description: DType is the data type to use
                    enum:
                    - float16
                    - bfloat16
                    - float32
                    type: string
                  enableLoRA:
                    description: EnableLoRA enables LoRA support
                    type: boolean
                  enableTool:
                    description: EnableTool enables automatic tool choice
                    type: boolean
                  maxModelLen:
                    description: MaxModelLen is the maximum model length
                    format: int32
                    type: integer
                  maxNumSeqs:
                    description: MaxNumSeqs is the maximum number of sequences to
                      process in parallel
                    format: int32
                    type: integer
                  modelURL:
                    description: ModelURL is the URL to download the model from
                    type: string
                  name:
                    description: Name is the name of the model to serve
                    type: string
                  path:
                    description: Path is the path to the model files
                    type: string
                  toolCallParser:
                    description: ToolCallParser specifies the tool call parser to
                      use
                    type: string
                  trustRemoteCode:
                    description: TrustRemoteCode enables loading custom model code
                    type: boolean
                required:
                - name
                type: object
              replicas:
                default: 1
                description: Replicas is the number of model serving replicas
                format: int32
                type: integer
              resources:
                description: Resources defines the resource requirements for each
                  replica
                properties:
                  cpu:
                    description: CPU is the CPU resource requirement
                    type: string
                  gpu:
                    default: "1"
                    description: GPU is the GPU resource requirement
                    type: string
                  memory:
                    description: Memory is the memory resource requirement
                    type: string
                type: object
              routerConfig:
                description: RouterConfig defines the router configuration
                properties:
                  routerType:
                    default: roundrobin
                    description: RouterType is the type of router to use
                    enum:
                    - roundrobin
                    - session
                    type: string
                type: object
              vllmConfig:
                description: VLLMConfig defines the vLLM-specific configuration
                properties:
                  enableChunkedPrefill:
                    description: EnableChunkedPrefill enables chunked prefill
                    type: boolean
                  enablePrefixCaching:
                    description: EnablePrefixCaching enables prefix caching
                    type: boolean
                  extraArgs:
                    description: ExtraArgs are additional command-line arguments to
                      pass to vLLM
                    items:
                      type: string
                    type: array
                  gpuMemoryUtilization:
                    description: GpuMemoryUtilization is the target GPU memory utilization
                      (0.0 to 1.0)
                    pattern: ^0(\.[0-9]+)?|1(\.0+)?$
                    type: string
                  hfTokenSecret:
                    description: HFTokenSecretName is the name of the secret to use
                      for pulling the image from Hugging Face
                    properties:
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  image:
                    description: Image defines the image configuration
                    properties:
                      name:
                        default: vllm/vllm-openai:latest
                        description: Name is the Docker image to use
                        type: string
                      pullPolicy:
                        default: IfNotPresent
                        description: PullPolicy is the policy for pulling the image
                        enum:
                        - Always
                        - IfNotPresent
                        - Never
                        type: string
                      pullSecretName:
                        description: PullSecretName is the name of the secret to use
                          for pulling the image
                        type: string
                      registry:
                        default: docker.io
                        description: Registry is the registry to use for pulling the
                          image
                        type: string
                    type: object
                  maxLoras:
                    description: MaxLoras is the maximum number of LoRAs to support
                    format: int32
                    type: integer
                  tensorParallelSize:
                    description: TensorParallelSize is the number of GPUs to use for
                      tensor parallelism
                    format: int32
                    type: integer
                  v1:
                    description: V1 enables v1 compatibility mode
                    type: boolean
                type: object
            required:
            - model
            type: object
          status:
            description: ProductionStackStatus defines the observed state of ProductionStack.
            properties:
              availableReplicas:
                description: AvailableReplicas is the number of available replicas
                format: int32
                type: integer
              conditions:
                description: Conditions represents the latest available observations
                  of the cluster's current state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              lastUpdated:
                description: LastUpdated is the last time the status was updated
                format: date-time
                type: string
              modelStatus:
                description: ModelStatus represents the status of the model loading
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
